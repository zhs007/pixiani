# Required for editor (Gemini)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# Optional proxy settings (any one of these)
HTTPS_PROXY=
HTTP_PROXY=
ALL_PROXY=
PROXY_URL=

# Example proxy values
# HTTPS_PROXY=http://1227.0.0.1:7890
# HTTPS_PROXY=http://user:password@proxy.example.com:8080

# --- Editor Server Advanced Settings ---

# Path to the system instruction file for the agent.
# If commented out, it defaults to 'apps/editor/prompts/system.md'.
# SYSTEM_INSTRUCTION_PATH=apps/editor/prompts/my_custom_prompt.md

# Agent stream idle timeout in milliseconds.
# The server will close the connection if no data is received from Gemini for this duration.
AGENT_CONTINUE_TIMEOUT_MS=30000

# Number of retries for the initial connection to the Gemini API stream.
AGENT_CONTINUE_RETRIES=1
